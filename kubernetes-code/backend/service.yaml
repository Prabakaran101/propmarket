apiVersion: v1
kind: Service
metadata:
  name: realestate-backend
#  name: backend-service
  namespace: propmarket
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
