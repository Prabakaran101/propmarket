apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: propmarket-role
  namespace: propmarket
rules:
  - apiGroups: [""]
    resources: ["configmaps", "secrets", "pods"]
    verbs: ["get", "list"]
